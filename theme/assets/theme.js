(() => {
const toggle=document.querySelector('.theme-toggle');
function sync(){const dark=document.documentElement.dataset.theme==='dark';toggle.textContent=dark?'☀ Light theme':'☾ Dark theme';toggle.setAttribute('aria-label',dark?'Switch to light theme':'Switch to dark theme');}
sync();toggle.addEventListener('click',()=>{const theme=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=theme;try{localStorage.setItem('fieldnotes-theme',theme);}catch(_){}sync();});
const menu=document.querySelector('.menu-button');menu.addEventListener('click',()=>{const open=document.body.classList.toggle('menu-open');menu.setAttribute('aria-expanded',String(open));});
document.addEventListener('keydown',event=>{if(event.key==='Escape'){document.body.classList.remove('menu-open');menu.setAttribute('aria-expanded','false');}});
if(window.renderMathInElement) document.querySelectorAll('.arithmatex').forEach(el=>renderMathInElement(el,{delimiters:[{left:'$$',right:'$$',display:true},{left:'\\[',right:'\\]',display:true},{left:'\\(',right:'\\)',display:false}],throwOnError:false,trust:false}));
function fallbackCopy(text){const area=document.createElement('textarea');area.value=text;area.style.position='fixed';area.style.opacity='0';document.body.append(area);area.select();const success=document.execCommand('copy');area.remove();if(!success)throw Error('Clipboard unavailable');}
document.querySelectorAll('article pre').forEach(pre=>{const button=document.createElement('button');button.className='copy-code';button.type='button';button.textContent='Copy';button.setAttribute('aria-label','Copy code');const code=pre.querySelector('code');const text=(code||pre).textContent;pre.append(button);button.addEventListener('click',async()=>{try{if(navigator.clipboard&&window.isSecureContext){try{await navigator.clipboard.writeText(text);}catch(_){fallbackCopy(text);}}else{fallbackCopy(text);}button.textContent='Copied!';}catch(_){const range=document.createRange();range.selectNodeContents(code||pre);const selection=window.getSelection();selection.removeAllRanges();selection.addRange(range);button.textContent='Press Ctrl+C';}setTimeout(()=>button.textContent='Copy',2200);});});
const input=document.querySelector('#search'),results=document.querySelector('#search-results');
input.addEventListener('input',()=>{const terms=input.value.trim().toLowerCase().split(/\s+/).filter(Boolean);results.replaceChildren();results.hidden=!terms.length;if(!terms.length)return;const matches=(window.FIELDNOTES_SEARCH||[]).map(entry=>({...entry,score:terms.reduce((score,term)=>score+(entry.title.toLowerCase().includes(term)?5:0),0)})).filter(entry=>terms.every(term=>(entry.title+' '+entry.text).toLowerCase().includes(term))).sort((a,b)=>b.score-a.score).slice(0,12);if(!matches.length){results.textContent='No matching pages.';return;}for(const entry of matches){const link=document.createElement('a');link.href=new URL(entry.url,new URL(document.body.dataset.root.replace(/\/?$/,'/'),location.href)).href;const title=document.createElement('strong');title.textContent=entry.title;const snippet=document.createElement('small');const at=Math.max(0,entry.text.toLowerCase().indexOf(terms[0])-35);snippet.textContent=entry.text.slice(at,at+145)+'…';link.append(title,snippet);results.append(link);}});
document.addEventListener('keydown',event=>{if(event.key==='/'&&!event.ctrlKey&&!event.metaKey&&!/INPUT|TEXTAREA|SELECT/.test(document.activeElement.tagName)&&!document.activeElement.isContentEditable){event.preventDefault();document.body.classList.add('menu-open');menu.setAttribute('aria-expanded','true');input.focus();}});
})();
